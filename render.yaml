services:
  - type: web
    name: fabric-dv-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app --workers 1 --threads 4 --timeout 60
    envVars:
      - key: DV_PUSH_API_KEY
        sync: false
